package admin

import "PORTal/templates/components"

templ AdminPage(dropdownValue string, content templ.Component) {
    <div class="admin-layout">
        @components.Dropdown(dropdownValue)
        <div id="admin-content" class="admin-content">
            @content
        </div>
    </div>
}